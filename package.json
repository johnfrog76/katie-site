{
  "scripts": {
    "clean": "rimraf dist",
    "start": "npm-run-all clean --parallel watch:build watch:server",
    "watch:build": "tsc --watch",
    "watch:server": "nodemon './dist/index.js' --watch './dist'"
  },
  "dependencies": {
    "@types/masonry-layout": "^4.2.7",
    "nodemon": "^3.1.0",
    "rimraf": "^5.0.5",
    "tsc": "^2.0.4"
  },
  "devDependencies": {
    "@types/jquery": "^3.5.29",
    "@types/lodash": "^4.17.0"
  }
}
